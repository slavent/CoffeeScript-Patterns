( ($) ->

	def_options = 
		speed: 500
		auto: true

	user_options =

	$.fn.slavent = (params) ->
		options = $.extend {}, $.fn.slavent.def_options, user_options

		init = () ->

			bindEvents = () ->

			bindEvents()

		makeStepPrev = () ->

		makeStepNext = () ->

		init()

		@

) jQuery